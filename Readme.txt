This ansible role installs Docker Engine CE on all existing nodes and different packages depending on Linux Distribution. 
It is to be installed from a controller.
It has integrated tests using molecule to test the code and see if the packages are installed correctly.

